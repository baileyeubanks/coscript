export default function VaultPage() {
  return (
    <main className="shell" style={{ paddingTop: "1rem" }}>
      <section className="panel">
        <div className="kicker">Vault</div>
        <h1 style={{ fontSize: "3rem" }}>Script memory</h1>
        <p className="muted">Saved briefs, generated variants, and approved script artifacts for reuse.</p>
      </section>
    </main>
  );
}

