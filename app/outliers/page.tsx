export default function OutliersPage() {
  return (
    <main className="shell" style={{ paddingTop: "1rem" }}>
      <section className="panel">
        <div className="kicker">Outliers</div>
        <h1 style={{ fontSize: "3rem" }}>Signal index</h1>
        <p className="muted">Ranked outliers with source context and score explanation fields.</p>
      </section>
    </main>
  );
}

